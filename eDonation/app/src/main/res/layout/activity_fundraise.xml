<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".FundraiseActivity">


    <include
        layout="@layout/toolbar"
        android:fitsSystemWindows="true"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_marginTop="55dp"
        >

        <Button
            android:id="@+id/createCampaignBtn"
            android:layout_width="280dp"
            android:layout_height="wrap_content"
            android:text="Create a campaign"
            android:layout_centerHorizontal="true"
            android:background="@drawable/outline_corners"
            android:fontFamily="sans-serif"
            android:textStyle="bold"
            android:textColor="@color/colorAccent"
            android:textSize="16sp"
            android:layout_marginTop="15dp"
            android:layout_gravity="center" />

        <Button
            android:id="@+id/viewCampaignBtn"
            android:layout_width="280dp"
            android:layout_height="wrap_content"
            android:text="My Campaigns"
            android:layout_centerHorizontal="true"
            android:background="@drawable/outline_corners"
            android:fontFamily="sans-serif"
            android:textStyle="bold"
            android:textColor="@color/colorAccent"
            android:textSize="16sp"
            android:layout_marginTop="15dp"
            android:layout_gravity="center" />

        <Button
            android:id="@+id/otherCampaignBtn"
            android:layout_width="280dp"
            android:layout_height="wrap_content"
            android:text="Other Campaigns"
            android:layout_centerHorizontal="true"
            android:background="@drawable/outline_corners"
            android:fontFamily="sans-serif"
            android:textStyle="bold"
            android:textColor="@color/colorAccent"
            android:textSize="16sp"
            android:layout_marginTop="15dp"
            android:layout_gravity="center" />

    </LinearLayout>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        app:menu="@menu/bottom_navigation"
        android:background="@color/colorWhite"
        app:labelVisibilityMode="labeled"
        app:itemTextColor="@drawable/bottom_navigation_item_colors"
        app:itemIconTint="@drawable/bottom_navigation_item_colors"/>



</RelativeLayout>